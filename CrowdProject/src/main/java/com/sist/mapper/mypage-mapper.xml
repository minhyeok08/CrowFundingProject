<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.mapper.MypageMapper">
	<select id="myPoint" parameterType="String" resultType="int">
	select point from wadiz_member where id=#{id}
	</select>
<!-- 	<select id="myCupon" parameterType="String" resultType="int">
	select count(*) from wadiz_my_cupon from id=#{id}
	</select>  -->
</mapper>